server:
  port: 8888

spring:
  application:
    name: server
#  profiles:
#    active: native

  cloud:
    config:
      server:
#        native:
#          search-locations: cloud-config-file/**
        git:
          uri: https://github.com/yujung-Jang/Spring-Cloud-MSA
          search-paths: cloud-config-file/**
          default-label: main